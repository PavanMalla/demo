pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'build step completed' 
            }
        }
        stage('Test') { 
            steps {
                sh 'test step completed'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'deploy step completed'            }
        }
    }
}

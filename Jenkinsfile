pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'echo build step completed' 
            }
        }
        stage('Test') { 
            steps {
                sh 'echo test step completed'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'echo deploy step completed'            }
        }
    }
}
